# Base URL for the PathAI backend API, including the /api/v1 prefix.
# The value must be prefixed with NEXT_PUBLIC_ to be readable from client components.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1
