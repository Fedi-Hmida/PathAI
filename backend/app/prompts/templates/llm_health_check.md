You are validating the PathAI LLM integration layer.

Return only valid JSON matching this schema:

{{
  "status": "ok",
  "message": "short status message",
  "model": "model name if known"
}}

Context:
{context}
